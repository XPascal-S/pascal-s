program sort (input, output);
  var a: array[0..10] of integer;
  x : integer;
procedure read_array;
  var i: integer;
  begin
    for i := 1 to 9 do read(a[i])
  end;
procedure exchange (i, j: integer);
  begin
    x := a[i]; a[i] := a[j]; a[j] := x;
  end;
procedure quick_sort(m, n: integer);
  var k, v: integer;
  begin
    {k = partition(m, n);
    quick_sort(m, k-1);
    quick_sort(k+1, n);}
    for i := m to n do write(a[i], ' ')
  end; { quick_sort end }
  begin read_array; exchange(2, 3); exchange(3, 5); exchange(6, 8); quick_sort(1, 9);
  end. {sort}