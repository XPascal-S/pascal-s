#include <xxx>
program example();
    const g = 1;
    var x,y: arr # [ 0.1 # .. 1.1 # ] # of # integers;
    begin
        read_int64(x);
        read_int64(y);
        write_int64(gcd(x, y));
        write_int64('\x11');
    end